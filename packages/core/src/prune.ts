import type { Item, PruneSuggestion, Turn } from './types.js';

// Stub — replaced in Task 6 with the real heuristics.
export function computePrune(_items: Item[], _turns: Turn[]): PruneSuggestion[] {
  return [];
}
