# Context-Budget-Visualizer
real-time "what's eating my context window" breakdown (which files, which tool outputs, which skill loads), with prune suggestions. The single biggest opaque cost in long sessions; the harness summarizes context but never shows you the composition.
